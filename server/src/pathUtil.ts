
import * as path from "path";
export const root = process.cwd()
export const src = path.join(root,'src')
export const client = path.join(root,'..','..','client')
export const serverDir = path.join(root,'..')